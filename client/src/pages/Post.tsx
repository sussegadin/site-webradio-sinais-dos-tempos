import { Link, useRoute } from 'wouter';
import { ArrowLeft, Calendar, Share2 } from 'lucide-react';
import { trpc } from '@/lib/trpc';
export default function Post(){const [,params]=useRoute('/post/:slug');const slug=params?.slug||'';const {data,isLoading}=trpc.blog.bySlug.useQuery({slug});if(isLoading)return <main className="container page-main"><div className="loading-state">Carregando matéria...</div></main>;const post=data||{title:'Fé em Tempos Difíceis',summary:'Uma palavra de esperança para sua vida.',content:'A oração e a fé são forças que nos sustentam em qualquer situação. Mesmo nos dias mais difíceis, existe esperança para quem escolhe confiar.',category:'Reflexão',imageUrl:undefined as string|undefined};return <main className="container article-page"><Link href="/blog" className="back-link"><ArrowLeft size={16}/> Voltar ao blog</Link><article><div className="article-meta"><span>{post.category}</span><small><Calendar size={14}/> Sinais dos Tempos</small></div><h1>{post.title}</h1><p className="article-lead">{post.summary}</p><div className="article-art" style={post.imageUrl?{backgroundImage:`url(${post.imageUrl})`,backgroundSize:'cover',backgroundPosition:'center'}:undefined}><div className="article-art-light"/></div><div className="article-content"><p>{post.content}</p><blockquote>“Quando estas coisas começarem a acontecer, olhai para cima, e levantai as vossas cabeças, porque a vossa redenção está próxima.”<cite>Lucas 21:28</cite></blockquote><button className="share-button"><Share2 size={16}/> Compartilhar matéria</button></div></article></main>}
